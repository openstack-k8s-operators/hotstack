---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Reference the remote architecture repo (includes upstream networker values)
components:
  - https://github.com/openstack-k8s-operators/architecture/examples/dt/uni01alpha/networker?ref=main

# Merge hotstack-specific overrides
patchesStrategicMerge:
  - hotstack-values.yaml
