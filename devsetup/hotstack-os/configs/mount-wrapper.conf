# mount.nfs wrapper configuration
# This file configures which NFS share to intercept and replace with bind mounts

# NFS share to intercept (format: server:/path)
NFS_SHARE=__NFS_SHARE__

# Local directory to bind mount instead
LOCAL_PATH=__NFS_LOCAL_PATH__
