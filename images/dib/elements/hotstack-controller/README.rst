================
hotstack-controller
================

DIB element for HotStack controller node image.

Installs packages needed for the hotstack controller node.

**Packages installed:**

- bash-completion, bind-utils, butane, dnsmasq, git, haproxy, httpd,
  httpd-tools, make, nfs-utils, nmstate, podman, tcpdump, tmux, vim-enhanced
